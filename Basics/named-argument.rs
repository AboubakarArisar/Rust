//A placeholder can take a named argument and assign it a value explicitly.
//It does this by specifying a name within the placeholder and assign that name the value to be inserted in the string.

fn main() {
    println!("{company} provides {kind} features", company = "Github", kind = "interactive");
}
